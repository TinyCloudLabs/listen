---
"listen-backend": patch
"listen-frontend": patch
---

Resolve Listen KV and SQL data paths from the app manifest so direct TinyCloud reads and backend writes use the manifest namespace consistently.
